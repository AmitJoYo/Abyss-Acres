## SnakeLighting — Stub. Lighting was used by the Abyss theme (now removed).
## Kept as a no-op so existing references don't break.
class_name SnakeLighting
extends Node

func setup(_controller: SnakeController) -> void:
	pass
